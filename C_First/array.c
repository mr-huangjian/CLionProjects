//
// Created by huangjian on 7/20/21.
//
#include <stdio.h>

/*int main() {
    int ages[5] = {10, 18, 25, 21, 30};
    for (int i = 0; i < 5; ++i) {
        printf("index: %d, age: %d\n", i, ages[i]);
    }

    printf("byte size: %d\n", sizeof(ages)); // 20
    printf("item size: %d\n", sizeof(ages) / sizeof(ages[0])); // 5

    return 0;
}*/

/**
 *  More https://www.runoob.com/cprogramming/c-arrays.html
 *  多维数组
 *  传递数组给函数
 *  从函数返回数组
 *  指向数组的指针
 */
