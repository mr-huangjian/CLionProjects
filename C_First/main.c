
#include <stdio.h>

#define PORT "8080"

/*int main(void) {
    printf("Hello, World!\n");
    printf("PORT: %s\n", PORT);

    const int length = 10;
    printf("length: %d\n", length);

    // & 获取变量的地址
    printf("address: %p\n", &length); // 0x7ffeee1e79a8

    // * 指向一个变量
    printf("length: %d\n", *&length); // 10

    // sizeof() 获取变量的大小
    printf("sizeof: %lu\n", sizeof(length)); // 4

    return 0;
}*/
